# if this gets big enough, it might make sense to split them into seprate files

alias ax='chmod a+x'
alias psts='pstree -s'
alias sour='source ~/.zshrc'
alias eg='env | grep -i'

# is this portable
alias get:ip='ipconfig getifaddr en0'

# macOS only
alias copy:ip='echo get:ip | pbcopy'

